Sub AdjustGSIProduction()
    Dim ws As Worksheet
    Dim i As Integer
    Dim lastRow As Long
    Dim sortCode As String
    Dim accountNumber As String
    
    
    
    ' Define the worksheet containing the data
    Set ws = ActiveSheet ' Adjust "Sheet1" to your sheet name
    
    ' Find the last row with data in the sheet
    lastRow = ws.Cells(ws.Rows.Count, "A").End(xlUp).Row
    
    ' Loop through each row from 2 to the last row
    For i = 2 To lastRow
        ' Get the sort code and account number
        sortCode = ws.Cells(i, 1).Value ' Assuming sort codes are in column A
        accountNumber = ws.Cells(i, 2).Value ' Assuming account numbers are in column B
        
        ' Enter sort code and account number into GSIProduction
        ' This part depends on how you interact with GSIProduction, for example:
        ' Activate GSIProduction window
        
        AppActivate "GSIProduction"
        
        Sleep 500
        SendKeys "{ESC}"
        Sleep 1000
        SendKeys "Term all"
        Sleep 1000
        SendKeys "~"
        Sleep 1000
        SendKeys "~"
        SendKeys "~"
        SendKeys "1"
        SendKeys "~"
        Sleep 1000
        SendKeys "19"
        SendKeys "~"
        Sleep 1000
        SendKeys "1"
        SendKeys "~"
        Sleep 1000
        SendKeys "20"
        'Sleep 1000
        ' Send keys to input the sort code and account number
        SendKeys sortCode
        SendKeys "~"
        Sleep 1000
        SendKeys "20"
        Sleep 1000
        SendKeys accountNumber
        Sleep 2000
        SendKeys "~"
        Sleep 1000
        SendKeys "04"
        Sleep 1000
        Sleep 1000
        SendKeys "~"
        SendKeys "+{TAB}+{TAB}+{TAB}+{TAB}", True
        Sleep 1000
        SendKeys "dec"
        Sleep 1000
        SendKeys "1.00"
        Sleep 1000
        SendKeys "{TAB}", True
        Sleep 1000
        SendKeys "01"
        Sleep 1000
        SendKeys "CHARGEBACK"
        Sleep 1000
        SendKeys "~"
        Sleep 1000
        SendKeys "~"
        SendKeys "~"
        Sleep 1000
        ' Wait for the application to process (adjust the delay as needed)
        Sleep 1000
        
        ' Perform the task adjustment in GSIProduction
        ' SendKeys "{F10}" ' Example adjustment command
        
        ' Wait for the adjustment to complete (adjust the delay as needed)
        Sleep 1000
        
        ' Add any additional steps here
        
    Next i
    
    MsgBox "Task adjustments completed for all entries.", vbInformation
End Sub
